import 'package:flutter/material.dart';

class FormTabScreen extends StatelessWidget {
  const FormTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('Form Tab Screen'),
        ),
      ),
    );
  }
}
