import 'package:flutter/material.dart';

class TableTabScreen extends StatelessWidget {
  const TableTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('Table Tab Screen'),
        ),
      ),
    );
  }
}
